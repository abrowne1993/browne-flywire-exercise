import { GetEmployeeResponseDto } from './get-employee-response-dto';

describe('GetEmployeeResponseDto', () => {
  it('should create an instance', () => {
    expect(new GetEmployeeResponseDto()).toBeTruthy();
  });
});
